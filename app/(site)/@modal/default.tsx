/**
 * The slot's fallback on a hard navigation: nothing. A refreshed or shared
 * `/work/[slug]` renders the full page in `children` and no modal.
 */
export default function ModalDefault() {
  return null;
}
