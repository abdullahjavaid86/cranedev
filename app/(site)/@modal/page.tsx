/**
 * Matches `/` inside the slot, so a client-side navigation home from an open
 * modal closes it. Without this the slot keeps its last active page and the
 * modal stays over the home page — see the catch-all beside this file.
 */
export default function ModalHome() {
  return null;
}
