# MILESTONES.md — progress tracker

Single source of truth for **what is done, what is in flight, and what is known-broken.**
Read at the start of every session. Update at the end of every task, before reporting.

Status: `todo` · `in progress` · `blocked` · `done`
A milestone is `done` only when every task in it passed the `quality-gate` skill. Date-stamp on completion (`YYYY-MM-DD`).

Each numbered row is **one commit** on `feature/fast-track` — the row is the review unit. See `CLAUDE.md §17` and the `git-workflow` skill. This file is updated in the same commit as the code it describes.

---

## M0 — Foundations

**Status:** done 2026-08-08

Stack as installed: **Next 16.3.0 · React 19.2.8 · TypeScript 5 (strict) · Tailwind 4 · motion 13 · lucide-react 1.30**. App Router, no `src/`, alias `@/*`, ESLint flat config.

| #   | Task                                                                              | Status          | Notes                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1 | `create-next-app` — App Router, TypeScript strict, Tailwind                       | done 2026-08-08 | Scaffolded in a temp dir and moved in, since `create-next-app` refuses a non-empty directory. Repo initialized by the scaffold (one commit, scaffold files only). `motion` + `lucide-react` installed; build and lint clean.                                            |
| 0.2 | Fonts via `next/font/google`: Bricolage Grotesque, Inter Tight, JetBrains Mono    | done 2026-08-08 | All three resolve as variable — no `weight` needed. Exposed as `--font-display` / `--font-body` / `--font-mono`. Placeholder metadata replaced; real agency name still pending (D2).                                                                                    |
| 0.3 | Tokens in `globals.css` + `@theme`; `lib/utils.ts` (`cn`)                         | done 2026-08-08 | Palette, radius, fluid type, easings. `cn()` is dependency-free per §10 and does **not** resolve conflicting utilities — revisit `tailwind-merge` when variant maps land in M1.5.                                                                                       |
| 0.4 | `lib/motion.ts` — ease, dur, spring, viewport                                     | done 2026-08-08 | Also carries `revealVariants`, `staggerVariants`, and `reduced()` so components never hand-roll a fifth reveal.                                                                                                                                                         |
| 0.5 | Grain overlay + `<Container>` in `app/layout.tsx`                                 | done 2026-08-08 | `Grain` is static and server-rendered — no client boundary. `Container` takes an `as` prop so sections keep correct semantics without a second wrapper.                                                                                                                 |
| 0.6 | `lib/api/client.ts` axios instance + `normalizeError`                             | done 2026-08-08 | One shape for every failure mode (HTTP/timeout/network), with `RATE_LIMITED` split out since GitHub 429s are routine.                                                                                                                                                   |
| 0.7 | Blank page proving every token renders                                            | done 2026-08-08 | Surfaces, ink, type scale, glass over a glow, radius, focus ring, content counts, motion values. Replaced wholesale in M2.                                                                                                                                              |
| 0.8 | Error surfaces — `error.tsx`, `global-error.tsx`, `not-found.tsx`; `.env.example` | done 2026-08-08 | Built on tokens only, no primitives needed. `global-error` renders its own `<html>`/`<body>` and degrades without the font variables.                                                                                                                                   |
| 0.9 | Install `zod` + `mongodb`; `lib/content/` loaders, `lib/db/client.ts`             | done 2026-08-08 | Official driver, not Mongoose. JSON files ship **empty** — honest until D2. Schema enforces kebab-case slugs and rejects an outcome line with no digit in it (§8). **Validation only runs when a page imports the loader** — an unimported content file is unvalidated. |

**Exit:** `yarn build` clean, blank page shows the palette, type scale, and grain.

---

## M1 — Primitives

**Status:** in progress

| #   | Task                                                                                             | Status          | Notes                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------ | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1 | `components/motion/Reveal.tsx`                                                                   | done 2026-08-08 | opacity + 24px + blur(6px)→0, `once: true`, `delay` prop. Reduced motion drops travel and blur entirely.                                                                                                                                                               |
| 1.2 | `components/motion/StaggerGroup.tsx`                                                             | done 2026-08-08 | Parent owns the trigger; `StaggerItem` reads reduced-motion from context so a child can never declare its own viewport.                                                                                                                                                |
| 1.3 | `components/motion/MaskedText.tsx` — line split                                                  | done 2026-08-08 | Lines are authored, not measured — a resize observer re-splitting mid-animation thrashes layout. Animated spans are `aria-hidden` with an `sr-only` copy of the full phrase.                                                                                           |
| 1.4 | `components/motion/Parallax.tsx`, `ScrollProgress.tsx`                                           | done 2026-08-08 | Added `hooks/useMediaQuery.ts` (+ `useIsDesktop`, `useHasHover`) — shared by every desktop-only enhancement. `ScrollProgress` animates `scaleX`, never width.                                                                                                          |
| 1.5 | `ui/Button`, `ui/Card`, `ui/Eyebrow`, `ui/Badge`, `ui/Field`                                     | done 2026-08-08 | `buttonStyles()` shared with button-styled links so there is one Button. `Button` extends `HTMLMotionProps`, not React's — the drag handler signatures collide. `Field` is a render-prop so input/textarea/select need no special-casing.                              |
| 1.6 | Reduced-motion verified on every primitive                                                       | done 2026-08-08 | Verified in headless Chrome over CDP, not by inspection: 12 runs (reduced on/off x light/dark x 360/768/1440). Zero infinite animations under `reduce`, zero content left stuck at opacity 0, zero horizontal overflow. Script pattern is reusable for M6.3/6.4.       |
| 1.9 | **Light + dark theming** — role-based tokens, `ThemeScript` (no FOUC), `useTheme`, `ThemeToggle` | done 2026-08-08 | Tokens renamed to roles; all components migrated. Accent split into fill (`--accent`) vs ink (`--accent-ink`) because cyan text on white is 1.43:1.                                                                                                                    |
| 1.7 | Scroll-reactive background layer (§4.6)                                                          | done 2026-08-08 | Three fields, transform+opacity only, blur and gradient static. Frozen at mid-scroll below `md` and under reduced motion by flattening the output range, not branching JSX. Ion capped at 0.14 — it is the token that muddies on light.                                |
| 1.8 | `<Modal />` primitive — focus trap, scroll lock, Escape, `router.back()`                         | done 2026-08-08 | Radix Dialog 1.1.23, behaviour only, restyled to role tokens. `forceMount` + `AnimatePresence` so motion owns the exit. Centred with auto margins, **not** translate — motion owns `transform`. No `next/navigation`: `onClose` is the whole routing contract for 5.2. |

**Exit:** every primitive renders correctly with `prefers-reduced-motion: reduce`.

---

## M2 — Shell + Hero

**Status:** in progress

| #   | Task                                                         | Status          | Notes                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------ | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1 | `layout/Header` — sticky, blurs past 40px, mobile overlay    | done 2026-08-08 | Radix Dialog for the overlay. Glass is a separate layer whose blur class is **absent** below 40px, not faded — a blurred layer at `opacity:0` still costs GPU. Scroll state via `useSyncExternalStore` on motion's `scrollY`, since `set-state-in-effect` now errors. |
| 2.2 | Shared nav source consumed by Header + Footer                | done 2026-08-08 | `lib/nav.ts` — primaryNav, primaryCta, footerNav, `isActive()`. Prefix match keeps Work active on `/work/[slug]`, including as an intercepted modal.                                                                                                                  |
| 2.3 | `layout/Footer`                                              | done 2026-08-08 | Server component. lucide v1 ships no brand glyphs, so GitHub is a labelled text link + `ArrowUpRight`. Email and GitHub handle are flagged `NEEDS CONFIRMING` (D8).                                                                                                   |
| 2.4 | Ship Log rail — section registration + scroll tracking       | done 2026-08-08 | Explicit array prop, not a context hook — a registration hook would force `'use client'` onto every section wrapper. IntersectionObserver for active section, one `useScroll` for the fill. Gated at `lg` (not `md`) since the rail needs a real gutter.              |
| 2.5 | Hero — masked headline, subcopy, CTA, ambient glows          | done 2026-08-08 | **Copy is DRAFT, needs rewriting (D2)** — deliberately contains no number, since every number here must be real. No hero glows: `ScrollBackground` already owns them site-wide.                                                                                       |
| 2.7 | `<WireSolid />` — cursor-tracking wireframe solid, hero only | done 2026-08-08 | Code-generated icosahedron (`lib/solid.ts`), 2 SVG paths on motion's shared frameloop. **No new dependency** — three+R3F+drei measured at 23MB/2.2MB/1.8MB unpacked. Paused off-screen, idle drift on touch, static under reduced motion, never takes the accent.     |
| 2.6 | Hero commit ticker (mocked data)                             | done 2026-08-08 | Data arrives as a prop, so M4.4 is a one-line swap. Rotation stops entirely under reduced motion. **Placeholder commits must not ship** — see Known gaps.                                                                                                             |

---

## M3 — Home body

**Status:** in progress

| #   | Task                                              | Status          | Notes                                                                                                                                                                                                                                                                                     |
| --- | ------------------------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1 | Proof strip — 4 real stats, count-up              | done 2026-08-08 | Count-up extracted to `ui/StatFigure` so the section stays a server component; server-rendered at final value, so it is correct with no JS.                                                                                                                                               |
| 3.2 | Services — card stacking, list on mobile          | done 2026-08-08 | List-first, stack added at `lg`. `lg:gap-48` is load-bearing — it makes the progress slices land on real hand-off points regardless of card height.                                                                                                                                       |
| 3.3 | Work grid — 4–6 projects, links to `/work/[slug]` | done 2026-08-08 | `ui/ProjectCard` is separate — /work and the modal both consume it. Remote covers use `fill` in a fixed aspect box; `sizes` set, no `priority`.                                                                                                                                           |
| 3.4 | Testimonials — attributed quotes only             | done 2026-08-08 | `<figure>`/`<blockquote>`/`<figcaption><cite>` — `<cite>` inside a quote would name the work, not the speaker.                                                                                                                                                                            |
| 3.5 | Brands / associations strip                       | done 2026-08-08 | Name is the primary visual (six identical placeholder marks would look broken). Mark is a CSS mask, not `next/image`, so `currentColor` survives.                                                                                                                                         |
| 3.6 | Process — `01→04`, tied to the rail               | done 2026-08-08 | Numbering derives from index; base is a plain list, `md` adds the rail.                                                                                                                                                                                                                   |
| 3.7 | Contact section + form → Server Action → MongoDB  | done 2026-08-09 | zod re-validated server-side, length caps, IP rate limit read from the same collection, honeypot answered with success. `BUDGET_BANDS` split into a zod-free module — importing it from the schema put zod in the client bundle (**-276KB**). Driver confirmed absent from client chunks. |
| 3.8 | CTA band                                          | done 2026-08-08 | The only section allowed the accent. CTA label/href from `lib/nav`.                                                                                                                                                                                                                       |

---

## M4 — GitHub integration

**Status:** in progress

| #   | Task                                                                        | Status          | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.1 | `app/api/github/route.ts` — server-only token, `revalidate = 3600`          | done 2026-09-24 | `force-static` + `revalidate = 3600`; returns `{ commits, openSource }` already fallen back, in our types only. **No client consumer yet** — the hero and the section are server-rendered off `lib/api/github.ts`, which enforces the same hour underneath. Kept as the sanctioned client path (§7.2); flagged so it is not mistaken for load-bearing.                                                                                                                                                                                                                                                                                                                                                  |
| 4.2 | `lib/api/github.ts` — typed, mapped to our types                            | done 2026-09-24 | Upstream bodies narrowed with zod before anything leaves the module; `types/index.ts` holds `Commit`, `Repo` derives from the content schema so live and fallback cannot drift. **The public events API no longer carries commit messages** (a PushEvent holds only `ref`/`head`/`before`), so recent commits come from `/repos/{repo}/commits` on the three most recently pushed repositories, merge commits skipped. "Pinned" is GraphQL-only and needs a token, so the REST approximation is own, non-fork, non-archived, most-starred. `unstable_cache` one hour, keyed by owner — four requests an hour, under the unauthenticated limit.                                                          |
| 4.3 | Open source section — pinned repos, stars, language, last push              | todo            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4.4 | "Recently shipped" panel switched from mock (`PLACEHOLDER_COMMITS`) to live | done 2026-09-24 | `PLACEHOLDER_COMMITS` deleted; the hero awaits `getRecentCommits()` and drops the panel column entirely when it is empty, so the copy never sits beside a blank. Awaited rather than streamed because the layout depends on the answer and the page is prerendered. `Commit.when` became `pushedAt` (ISO) formatted at render by the one `formatRelative()` in `lib/utils.ts`. Home page `revalidate = 3600`. Until D3 is answered the panel is hidden in production — which is the honest state, not a bug.                                                                                                                                                                                            |
| 4.5 | Three states + typed `content/` fallback on failure                         | done 2026-09-24 | `content/opensource.json` (sample data, listed in `content/README.md`) carries no star counts or push dates — a card shows fewer facts rather than invented ones. Commits have **no** fallback on purpose. Blank `GITHUB_OWNER` makes no request at all, which keeps CI off the network. Verified by build: a 404 owner logs server-side and renders the fallback; a 403 rate limit mid-feed keeps the live grid and hides the panel. No `Suspense` around either read — on a prerendered page it produced an out-of-order chunk (skeleton inline, cards appended behind a swap script) for a skeleton no visitor can see. Loading is therefore not a designed state here; live / fallback / empty are. |

---

## M5 — Routes

**Status:** in progress

| #   | Task                                                     | Status | Notes                                                                                           |
| --- | -------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------- |
| 5.1 | `/work` — index, filterable                              | todo   |                                                                                                 |
| 5.2 | `/work/[slug]` — full page + `@modal` intercepting route | todo   | `generateStaticParams` + `notFound`. Modal on client nav, full page on direct load/share (§6.2) |
| 5.3 | `/about`                                                 | todo   |                                                                                                 |
| 5.4 | `/team`                                                  | todo   | real people only                                                                                |
| 5.5 | `/contact`                                               | todo   |                                                                                                 |
| 5.6 | `/schedule` — slot picker, mocked submit                 | todo   | third-party embed needs a decision                                                              |
| 5.7 | `/careers`                                               | todo   | state comp bands                                                                                |
| 5.8 | `/careers/[slug]` + application form                     | todo   |                                                                                                 |
| 5.9 | `not-found.tsx`                                          | todo   |                                                                                                 |

---

## M6 — Polish

**Status:** in progress

| #   | Task                                                                      | Status | Notes                                                                  |
| --- | ------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------- |
| 6.1 | Page transitions via `AnimatePresence`                                    | todo   | must not reset the ambient scene's scroll math                         |
| 6.2 | Metadata + OG images per route                                            | todo   |                                                                        |
| 6.3 | Responsive pass — 360 / 390 / 768 / 1024 / 1280 / 1440 / 1920 + landscape | todo   | audit, not the strategy; mobile-first is enforced per section as built |
| 6.4 | Keyboard + contrast pass, all routes                                      | todo   |                                                                        |
| 6.5 | Lighthouse — Perf ≥ 90, A11y 100 on `/`                                   | todo   |                                                                        |
| 6.6 | Chanel pass — remove one effect per section, keep what survives           | todo   |                                                                        |

---

## M7 — Visual revamp

**Status:** done 2026-09-12

The owner asked for a quieter, more "OS-like" visual direction: one soft indigo accent, Geist Sans/Geist Mono in place of the three-typeface set, an ambient `three.js` scene in place of the Ship Log rail and reading panel, and a frosted-glass treatment instead of the neon-obsidian look. See `docs/superpowers/specs/2026-09-12-visual-revamp-design.md`.

| #   | Task                                                                          | Status          | Notes                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------------------------------------------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 7.1 | Tokens, fonts, glass recipe, primitives (Eyebrow, Card, Badge, Button, Field) | done 2026-09-12 | Indigo accent tokens, Geist Sans/Geist Mono via `next/font/google`, `--glass-fill/catch/drop`, radius `10/16/24`, `--scene-a/b/c` + `--scene-pane`. `--ion` and the old field/lattice tokens deleted.                                                                                                                                                                                                                               |
| 7.2 | Shell: header, scroll line, footer, `Scene` with `three`, deletions           | done 2026-09-12 | `Scene` measured at **536,604 bytes raw / 132,508 bytes gzipped**, loaded after first paint via `requestIdleCallback`, absent from `/`'s first-load JS. Ship Log rail, reading panel, wireframe solid, and the three CSS background variants all removed.                                                                                                                                                                           |
| 7.3 | Hero with the "Recently shipped" panel                                        | done 2026-09-12 | Headline changed to "Built to ship." / "Built to last." — still a draft (D2) — because the rotating commit ticker it replaces no longer justifies a live-feeling headline; a static shipped-work panel reads better paired with settled copy.                                                                                                                                                                                       |
| 7.4 | Home body: Proof, Services, Work, Testimonials, Brands, Process, Contact, CTA | done 2026-09-12 | Services moved from sticky card-stacking to a two-column split; Work's first project now spans the grid; Testimonials leads with one featured quote.                                                                                                                                                                                                                                                                                |
| 7.5 | Cleanup of dead modules, docs and skills, CI guards, tracker                  | done 2026-09-12 | `CLAUDE.md` and the `design-system`, `motion-system`, `building-a-section`, `adding-a-page`, `quality-gate` skills rewritten to the indigo/glass direction. See the skill audit row below. Also fixed a Task 1 regression: a hand-written `-webkit-backdrop-filter` made Lightning CSS drop the standard property, so no glass surface blurred in Chrome; the prefix is now left to the compiler. `Parallax` deleted (no importer). |

---

## Open decisions

Things that need a human answer. Do not guess past these.

| #   | Question                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Raised     | Status                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | `/schedule`: self-built slot picker, or embed a third-party scheduler (a dependency + design-consistency call)?                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-08 | open                                                                                                                                                                                                                 |
| D2  | Real content — client names, project outcomes, stats, testimonials, logos, team bios, comp bands. Every one of these must be real.                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-08 | open                                                                                                                                                                                                                 |
| D3  | Which GitHub org/user feeds the hero's "Recently shipped" panel and the open-source section?                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-08 | open                                                                                                                                                                                                                 |
| D5  | MongoDB hosting — Atlas or self-hosted? Affects `MONGODB_URI` and whether IP allow-listing is needed. `.env.example` covers both forms.                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-08 | open                                                                                                                                                                                                                 |
| D11 | **Vercel project settings** — the parts that cannot live in the repo: `ENABLE_EXPERIMENTAL_COREPACK` fallback, all five env vars (with a **separate Mongo database for Preview**), Node version, and production branch = `main`. See `DEPLOYMENT.md`.                                                                                                                                                                                                                                                                                                                    | 2026-08-09 | open                                                                                                                                                                                                                 |
| D12 | **No Content-Security-Policy.** `ThemeScript` must be inline to resolve the theme before paint, so a CSP without a nonce would block it. Adding one means plumbing a nonce through the document.                                                                                                                                                                                                                                                                                                                                                                         | 2026-08-09 | open                                                                                                                                                                                                                 |
| D10 | **Reading panel (prototype).** Fills the otherwise-dead right gutter past 1400px with a live "currently reading" block. Keep, change, or drop. If kept, lift the IntersectionObserver into one provider — the rail and the panel currently run one each over the same seven sections.                                                                                                                                                                                                                                                                                    | 2026-08-09 | closed 2026-09-12 — dropped with the Ship Log rail. M7 deletes both; the single-column layout has no gutter left to fill.                                                                                            |
| D9  | **Ambient background + hero shape.** Both live behind a picker (bottom-right) that **ships in production** by request, so the choice can be made on the deployed site — it is visible to visitors until this is decided. Backgrounds: `fields` (blurred gradients), `lattice` (projected point field), `grid` (perspective plane), `none`. Hero shapes: `knot`, `globe`, `icosahedron`, `helix`, `vortex`. Pick one of each, then delete the losers plus `useDomFlag`, `useBackground`, `useHeroShape`, `DevVariantPicker`, `BackgroundLayer` and the ThemeScript lines. | 2026-08-08 | closed 2026-09-12 — all variants deleted, no single one chosen. M7 replaces the picker and every option with one `three.js` ambient scene (`components/layout/Scene.tsx`), so there is nothing left to pick between. |
| D8  | Footer contact details are placeholders: `hello@cranedev.com` and `github.com/cranedev`. The real domain is `cranedev.ai.studio`, so both are likely wrong. `GITHUB_OWNER` in `.env.example` is blank too.                                                                                                                                                                                                                                                                                                                                                               | 2026-08-08 | open                                                                                                                                                                                                                 |
| D7  | Light mode changes the ambient glows and the Ship Log rail's look — both were designed against `--void`. Worth your eye once M2 lands.                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-08-08 | closed 2026-09-12 — moot. Both the ambient glows and the Ship Log rail are gone; light mode was redesigned as part of M7 against the new indigo/glass tokens directly, not retrofitted from the dark-first look.     |
| D6  | Submissions are write-only with no admin UI, so nothing in the app reads them back. How do you want to be notified of a new contact query — email, Slack, or checking the collection directly?                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-08 | open                                                                                                                                                                                                                 |
| D4  | Ship Log below `lg`: proposed a 2px cyan progress bar under the header, with the active section's hash/number in that section's eyebrow. The rail has no gutter to pin to at 360px. Written into CLAUDE.md §4.5 as the default — flag if you want a different mobile form for the signature element.                                                                                                                                                                                                                                                                     | 2026-08-08 | closed 2026-09-12 — the rail itself is gone (M7), so the question no longer applies. The 2px scroll line survives as the accent progress indicator, on every size, not just below `lg`.                              |

---

## Known gaps

Real problems we shipped past on purpose. Not a wishlist.

- **Next.js is on 16.3.4, not the current 16.3.5 (2026-09-12).** 16.3.4 clears both critical advisories (GHSA-p293-qw3h-jr36, unauthenticated RCE on Windows-hosted servers; GHSA-2xp9-vwfh-vxw4, unauthenticated RCE in the Image Optimization API on AVIF input) — both are patched at 16.3.3. 16.3.5 was published 16 hours before the bump and Yarn's publish-age gate quarantines it, which is the guard working as intended; overriding a supply-chain check to chase a day-old release during a security fix is the wrong trade. Move to 16.3.5 once it ages past the gate. `sharp` came to 0.35.4 with it (GHSA-rgj7-g3m4-5g8c, libheif), which is the patched version.

- **`main` still carries a stale `package-lock.json`.** It is the reason each Next.js advisory is reported twice — once against `package.json`, once against the npm lockfile. The file was deleted on `feature/fast-track` in `ec486e8` when the project moved to Yarn Berry, so a normal `feature/fast-track -> staging -> main` release removes it and closes the duplicate alerts. Dependabot PR #8 targets `main` and _updates_ that lockfile instead — merging it would re-establish an npm lockfile alongside `yarn.lock` (§2). Close it rather than merge it.

- **M7 final review: 21 minor findings deferred (2026-09-12).** The eight must-fix items were fixed on the branch; these were not. Grouped: (a) _accent discipline_ — the scroll-progress line, the header CTA and the hero CTA are three accent elements in the first viewport (§4.1 says one); (b) _scene_ — resizing across `md` disposes and rebuilds the WebGL context (`Scene.tsx` effect keyed on `animate`); `uTime` is unbounded; `parseRgba` only handles the exact token shape; `@types/three` is 0.185 against `three` 0.186; (c) _dead or duplicated code_ — `useHasHover` has no importer; `app/not-found.tsx` hand-rolls three copies of the secondary button; the `ghost` button has no `active:` state for touch; (d) _a11y and scale_ — `ShippedPanel`'s list has no accessible name and its sha uses `text-xs`, off the type scale; (e) _stale text_ — comments in `ProjectCard`, `useMediaQuery`, `Contact`, `lib/utils.ts`; "Ship Log" mentions in `DEPLOYMENT.md` and the `admin-portal` skill; `CLAUDE.md` §9 still names `--mist`, §4.6 forgets `ScrollProgress` reads page progress, §4.3 says stat figures are mono (they are not); this file's "no admin UI" gap is outdated (the portal exists). Fix as a single sweep before M5 starts.

- **The contact form writes to MongoDB but nothing reads it back.** There is no admin UI by design (§2), so a submission is invisible until someone queries the collection. See D6 — a notification path was never chosen.
- **ALL of `content/` is invented sample data.** See `content/README.md` for the per-file list. Authorised by the owner so the sections have a correct shape to build against, and to be replaced before launch. The numbers in `work.json` and `stats.json` are fabricated, and `testimonials.json` attributes invented quotes to invented people — that is the entry with the most reputational weight if it ever ships.
- **Project covers and team photos are remote Unsplash URLs.** They must become static imports from `/public` for the blur placeholder and intrinsic sizing (§7.0) — a schema change, not just a value swap. `next.config.ts` `remotePatterns` exists only to serve them and should be removed with them.
- **Hero copy is a draft.** Headline and subcopy in `components/sections/Hero.tsx` were written to the §8 rules but describe a team whose actual positioning has not been supplied (D2).
- **Six links on the home page point at routes that do not exist.** `/work`, `/about`, `/team`, `/careers`, `/contact` and `/schedule` all return 404 in production — the header and footer link to them, and M5 has not been built. This is now the largest SEO problem on the site: a homepage whose internal links are dead is a crawl-quality signal, and the sitemap can only honestly contain one URL. Fixing the metrics does not touch it.
- **`NEXT_PUBLIC_SITE_URL` was never set in Vercel, and the code defaulted to localhost.** The live `sitemap.xml` advertised `http://localhost:3000` and `robots.txt` pointed its Sitemap directive there, so the sitemap was unfetchable by any crawler. Fixed by resolving the origin in `lib/seo.ts` with a `VERCEL_PROJECT_PRODUCTION_URL` fallback, so it is correct on Vercel with nothing configured. **Still set `NEXT_PUBLIC_SITE_URL` when a real domain exists** — the current canonical is the `.vercel.app` hostname.
- **TTFB is network-bound and was never the problem.** It reads 0.75s at field P75 and is already GREEN. Measured against the deployment: `/` is an edge cache HIT (`x-vercel-cache: HIT`), Brotli, HTTP/2, no redirect on HTTPS, and the proxy does not run on it. The breakdown from a ~100ms-RTT client is DNS 3ms, TCP 100ms, TLS +105ms, server +105ms — two thirds of it is connection setup, which application code does not control. **There is no code change that improves it**, and it is now the floor for everything else: LCP equals FCP, and FCP cannot begin before the server responds. The one remaining lever is browser caching for REPEAT visits (`Cache-Control` is `max-age=0, must-revalidate`, so every return visit pays a revalidation round trip). Adding `stale-while-revalidate` would help returning visitors and does nothing for first views — it trades up-to-N-seconds of staleness after a deploy, and is an open decision rather than an obvious win.
- **Core Web Vitals were fixed in the lab, not yet confirmed in the field.** Field P75 on 2026-08-09 was LCP 4.47s (poor), FCP 2.37s, INP 272ms. Three root causes were found by measurement and fixed: the hero's largest text waited for hydration (LCP), 125KB of font preloads sat ahead of the 10KB stylesheet that actually blocks paint (FCP), and the blurred ambient fields re-ran a 120px blur every frame because their layers were never promoted (INP — 640ms worst interaction vs 48ms once promoted). Lab now: LCP == FCP at every CPU tier, 576ms on a 1.6Mbps link; worst interaction 48–56ms; CLS 0.0001. **TTFB (0.75s field) is untouched and is now the floor** — LCP can only be as good as FCP, and FCP starts after the server responds. Re-read the dashboard once real traffic accumulates; the original sample was ~14 loads, so P75 was 3–4 page views wide.

---

## Skill audits

One line per milestone, per `maintaining-skills`. A no-op audit is a valid entry.

| Date       | Milestone | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-08 | —         | Skills authored: design-system, motion-system, building-a-section, adding-a-page, content-and-copy, data-and-forms, quality-gate, maintaining-skills.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-08 | M2        | **`design-system` corrected: a token that works on dark does not work on light by symmetry.** The scroll fields were invisible in light mode — a bright field at 0.14 alpha is a 2.8x luminance step on near-black and 1.04x on `#FAFBFC`. Fix: bake the alpha into themed `--field-*` tokens and let motion animate only a relative band.                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-08 | M0.1      | `motion-system` corrected: package is `motion`, not `framer-motion` (no `./react` subpath on the latter). `CLAUDE.md §2` stack row updated to match, and a Next 16 warning added at the top of `CLAUDE.md` pointing at `node_modules/next/dist/docs/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-08 | —         | **Git workflow adopted.** New `git-workflow` skill + `CLAUDE.md §17`: one branch (`feature/fast-track`), one commit per tracker row. Branch created; existing work committed as two commits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-08 | —         | **Architecture change: backend added.** §2 constraints reversed — MongoDB for submissions (write-only), JSON + zod for page content, server-first with Server Actions. New `data-persistence` skill. §6.2 project details via parallel + intercepting routes (modal on client nav, full page on direct load). §4.6 scroll-reactive background. `.env.example` written.                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-08 | —         | **§2 constraints relaxed.** shadcn/Radix permitted on demand for hard interactive behaviour (never MUI/Chakra), adopted for behaviour only and restyled to our tokens. Non-`motion` animation libraries allowed only for a named capability motion lacks. No-duplication broadened from components to hooks, helpers, types, and logic. Propagated to `building-a-section`, `motion-system`, §10.                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-08 | —         | **Adopted from `biosum/admin` skills.** Async `params`/`searchParams` (Next 15+/16) → `adding-a-page`; route state files (`loading`/`error`/`not-found` + Suspense) → `adding-a-page` + `data-and-forms`; search-before-you-build component policy + prop conventions → `building-a-section` + §10 + §15; static image imports, `priority`/LCP, `remotePatterns` → `building-a-section` + `quality-gate`; discriminated-union async state, `unknown` at boundaries, no `!` → `data-and-forms` + §10; `hooks/` added to the tree. **Not adopted:** Supabase/RLS/HIPAA/Stripe/n8n/storage/deploy (no backend here), their caching skill (PHI-specific), their `git` skill (mandates never auto-commit — this project chose the opposite). |
| 2026-08-08 | —         | **PR base corrected to `staging`.** `git-workflow` + §17 updated: every PR targets `staging`; `main` is a base only on explicit request in that request. Topology `main ← staging ← feature/fast-track`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-08 | —         | **Mobile-first adopted as a project constraint.** New `CLAUDE.md §4.6` (authoring rule); §4.2 glass budget split by breakpoint; §4.3 display floor lowered `3.5rem → 2.5rem`; §4.5 mobile Ship Log defined; §9 breakpoint matrix widened. Propagated to `design-system`, `motion-system`, `building-a-section`, `adding-a-page`, `quality-gate`.                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-12 | M7        | **Visual direction changed: neon obsidian → quiet obsidian, one indigo, frosted glass.** `CLAUDE.md` rewritten wholesale (§2 stack, §4 design system, §5 motion library rule, §6.1 home, §11, §12). `design-system`, `motion-system`, `building-a-section`, `adding-a-page`, and `quality-gate` skills rewritten to match: Ship Log rail, reading panel, cyan/ion accent, Bricolage Grotesque/Inter Tight/JetBrains Mono, card stacking, and the cursor light are all gone from the live rules — they remain only as history in this table and in closed decisions D4/D7/D9/D10.                                                                                                                                                        |
